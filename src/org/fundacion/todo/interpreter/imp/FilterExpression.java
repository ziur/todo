package org.fundacion.todo.interpreter.imp;

import org.fundacion.todo.interpreter.Command;
import org.fundacion.todo.interpreter.Context;
import org.fundacion.todo.interpreter.Interpreter;

public class FilterExpression implements Interpreter {
    @Override
    public Command interpreter(Context context) {
        return null;
    }
}
